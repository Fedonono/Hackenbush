var EdgeUI = function( color, start, goal, index) {
	
	this.color = color;
	this.start = start; // id
	this.goal = goal;   // id
        this.index = index  // his index in the edge table containig all edges between two nodes.
	
}
